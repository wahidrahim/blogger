<h1><%= @article.title %></h1>
<p><%= @article.body %></p>
<%= link_to "<< Back to Article list", articles_path %>
<%= link_to "delete", article_path(@article.id), method: :delete, data: {confirm: "Are you sure you want to delete: #{@article.title}"} %>
<%= link_to "edit", edit_article_path(@article.id) %>
